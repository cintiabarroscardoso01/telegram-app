{
  "name": "telegram-app-loader",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram app loader with browser bundle build for GitHub Pages",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "telegram": "^2.26.22"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
